name: CD Pipeline

on:
  workflow_run:
    workflows: ["CI Pipeline"]
    types:
      - completed

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Pull Docker Image
      run: docker stack deploy -c ../app/docker-compose.yml -c ../app/docker-compose.prod.yml node-app
